"use client";

import Link from "next/link";
import { AlertTriangle, ArrowLeft, FileDown, LockKeyhole, RefreshCw, ShieldCheck, UserPlus, Users } from "lucide-react";
import { FormEvent, useCallback, useEffect, useMemo, useState } from "react";
import { cameraInventoryApi, organizationApi, secureAreaAuthorizationApi } from "@/lib/api-client";
import type { Branch } from "@/lib/types";

type Area = "cash_counter" | "locker";
const today = () => new globalThis.Date().toISOString().slice(0, 10);

export default function AuthorizedPersonsPage() {
  const [branches, setBranches] = useState<Branch[]>([]); const [locations, setLocations] = useState<any[]>([]); const [branchId, setBranchId] = useState(""); const [locationId, setLocationId] = useState("");
  const [persons, setPersons] = useState<any[]>([]); const [assignments, setAssignments] = useState<any[]>([]); const [report, setReport] = useState<any[]>([]); const [alerts, setAlerts] = useState<any[]>([]);
  const [from, setFrom] = useState(today()); const [to, setTo] = useState(today()); const [busy, setBusy] = useState(false); const [message, setMessage] = useState("");
  const scope = useMemo(() => ({ branchId, ...(locationId ? { locationId } : {}) }), [branchId, locationId]);
  const load = useCallback(async () => { if (!branchId) return; setBusy(true); try { const [p, a, r, l] = await Promise.all([secureAreaAuthorizationApi.listPersons(scope), secureAreaAuthorizationApi.listAssignments({ ...scope, date: today() }), secureAreaAuthorizationApi.datewiseReport({ ...scope, from, to }), secureAreaAuthorizationApi.lockerChangeAlerts({ ...scope, from, to })]); setPersons(p.data); setAssignments(a.data); setReport(r.data); setAlerts(l.data); setMessage(""); } catch (error) { setMessage(readable(error)); } finally { setBusy(false); } }, [branchId, scope, from, to]);
  useEffect(() => { void Promise.all([cameraInventoryApi.listBranches("analytics:view"), organizationApi.listNodes({ type: "location" })]).then(([branchResponse, locationResponse]) => { setBranches(branchResponse.data as Branch[]); setBranchId((branchResponse.data as Branch[])[0]?.id ?? ""); setLocations(locationResponse.data); }).catch((e) => setMessage(readable(e))); }, []);
  useEffect(() => { void load(); }, [load]);
  const downloadReport = () => { const rows = [["Date", "Area type", "Area", "Authorized person", "Employee code", "Designation", "Effective from", "Effective until", "Reason"], ...report.map((x) => [x.date, x.areaType, x.areaName, x.fullName, x.employeeCode, x.designation ?? "", x.effectiveFrom, x.effectiveUntil ?? "", x.changeReason ?? ""])]; const blob = new Blob([rows.map((row) => row.map(csv).join(",")).join("\n")], { type: "text/csv" }); const url = URL.createObjectURL(blob); const a = document.createElement("a"); a.href = url; a.download = `secure-area-authorizations-${from}-to-${to}.csv`; a.click(); URL.revokeObjectURL(url); };
  return <main className="min-h-[calc(100vh-5rem)] bg-slate-950 p-4 text-slate-100 xl:p-6"><header className="mb-5 flex flex-wrap items-start justify-between gap-4 rounded-3xl border border-slate-800 bg-slate-900 p-6"><div className="flex gap-4"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-emerald-500/10 text-emerald-300"><LockKeyhole /></span><div><Link href="/analytics/banking" className="flex items-center gap-1 text-xs text-slate-400 hover:text-white"><ArrowLeft size={13} /> Banking analytics</Link><h1 className="mt-2 text-2xl font-bold">Cash counter & locker authorization</h1><p className="mt-1 text-sm text-slate-400">Register authorized staff by branch or location. Counter assignment can be changed daily; every locker replacement creates a separate alert report.</p></div></div><div className="flex flex-wrap items-end gap-2"><Select label="Branch" value={branchId} setValue={setBranchId} options={branches.map((b) => ({ value: b.id, label: b.name }))} /><Select label="Location scope" value={locationId} setValue={setLocationId} options={[{ value: "", label: "Entire branch" }, ...locations.map((location) => ({ value: location.id, label: location.name }))]} /><button onClick={() => void load()} className="rounded-lg border border-slate-700 p-2.5"><RefreshCw size={16} className={busy ? "animate-spin" : ""} /></button></div></header>
    {message && <p className="mb-4 flex gap-2 rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-200"><AlertTriangle size={17} />{message}</p>}
    {!branchId ? <p className="rounded-xl border border-slate-800 p-6 text-slate-400">No accessible branch is available.</p> : <div className="grid gap-5 xl:grid-cols-2"><PersonForm scope={scope} onSaved={load} /><AssignmentForm scope={scope} persons={persons} onSaved={load} /><section className="overflow-hidden rounded-2xl border border-slate-800 bg-slate-900 xl:col-span-2"><header className="flex flex-wrap items-end justify-between gap-3 border-b border-slate-800 p-4"><div><h2 className="font-semibold">Date-wise authorization report</h2><p className="text-xs text-slate-500">Cash counter and locker authorization history</p></div><div className="flex items-end gap-2"><Date label="From" value={from} setValue={setFrom} /><Date label="To" value={to} setValue={setTo} /><button onClick={downloadReport} disabled={!report.length} className="flex items-center gap-2 rounded-lg bg-blue-600 px-3 py-2 text-xs font-bold disabled:opacity-40"><FileDown size={14} /> CSV</button></div></header><Rows rows={report} empty="No authorizations for the selected period." columns={[["Date", "date"], ["Area", "areaType"], ["Counter / locker", "areaName"], ["Authorized person", "fullName"], ["Employee", "employeeCode"], ["Reason", "changeReason"]]} /></section><section className="overflow-hidden rounded-2xl border border-amber-500/25 bg-slate-900 xl:col-span-2"><header className="border-b border-amber-500/20 p-4"><h2 className="flex items-center gap-2 font-semibold text-amber-200"><AlertTriangle size={16} /> Locker authorization change alerts</h2><p className="mt-1 text-xs text-slate-500">Separate audit report generated whenever an existing locker authority is replaced.</p></header><Rows rows={alerts} empty="No locker authorization changes for this period." columns={[["Locker", "lockerName"], ["Previous", "previousPerson"], ["New", "newPerson"], ["Reason", "changeReason"], ["Changed at", "createdAt"]]} /></section><section className="overflow-hidden rounded-2xl border border-slate-800 bg-slate-900 xl:col-span-2"><header className="border-b border-slate-800 p-4"><h2 className="flex items-center gap-2 font-semibold"><ShieldCheck size={16} /> Today’s active authorizations</h2></header><Rows rows={assignments} empty="No active authorization recorded today." columns={[["Area", "areaType"], ["Counter / locker", "areaName"], ["Person", "fullName"], ["Employee", "employeeCode"], ["Effective from", "effectiveFrom"]]} /></section></div>}</main>;
}

function PersonForm({ scope, onSaved }: { scope: any; onSaved: () => Promise<void> }) { const [name, setName] = useState(""); const [code, setCode] = useState(""); const [designation, setDesignation] = useState(""); const [saving, setSaving] = useState(false); const submit = async (e: FormEvent) => { e.preventDefault(); setSaving(true); try { await secureAreaAuthorizationApi.registerPerson({ ...scope, fullName: name, employeeCode: code, designation: designation || undefined }); setName(""); setCode(""); setDesignation(""); await onSaved(); } finally { setSaving(false); } }; return <section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="flex items-center gap-2 font-semibold"><UserPlus size={17} /> Authorized person registration</h2><form onSubmit={submit} className="mt-4 grid gap-3 sm:grid-cols-3"><Input label="Full name" value={name} setValue={setName} required /><Input label="Employee code" value={code} setValue={setCode} required /><Input label="Designation" value={designation} setValue={setDesignation} /><button disabled={saving} className="rounded-xl bg-emerald-600 px-4 py-2 text-xs font-bold sm:col-span-3">{saving ? "Saving…" : "Register authorized person"}</button></form></section>; }
function AssignmentForm({ scope, persons, onSaved }: { scope: any; persons: any[]; onSaved: () => Promise<void> }) { const [type, setType] = useState<Area>("cash_counter"); const [area, setArea] = useState(""); const [person, setPerson] = useState(""); const [date, setDate] = useState(today()); const [reason, setReason] = useState(""); const [saving, setSaving] = useState(false); const submit = async (e: FormEvent) => { e.preventDefault(); setSaving(true); try { const result = await secureAreaAuthorizationApi.assign({ ...scope, areaType: type, areaName: area, authorizedPersonId: person, effectiveDate: date, changeReason: reason || undefined, replaceCurrent: true }); setArea(""); setReason(""); await onSaved(); if (result.lockerChangeAlertId) window.alert("Locker authorization changed. A separate alert report was generated."); } finally { setSaving(false); } }; return <section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="flex items-center gap-2 font-semibold"><Users size={17} /> Daily assignment / change</h2><form onSubmit={submit} className="mt-4 grid gap-3 sm:grid-cols-2"><Select label="Area" value={type} setValue={(v) => setType(v as Area)} options={[{ value: "cash_counter", label: "Cash counter" }, { value: "locker", label: "Locker" }]} /><Input label="Counter / locker name" value={area} setValue={setArea} required /><Select label="Authorized person" value={person} setValue={setPerson} options={[{ value: "", label: persons.length ? "Select person" : "Register a person first" }, ...persons.map((p) => ({ value: p.id, label: `${p.fullName} (${p.employeeCode})` }))]} /><Date label="Effective date" value={date} setValue={setDate} /><Input label={type === "locker" ? "Change reason (required)" : "Handover reason"} value={reason} setValue={setReason} required={type === "locker"} /><button disabled={saving || !person} className="rounded-xl bg-blue-600 px-4 py-2 text-xs font-bold sm:col-span-2">{saving ? "Assigning…" : type === "cash_counter" ? "Set daily counter person" : "Change locker authority"}</button></form></section>; }
function Rows({ rows, columns, empty }: { rows: any[]; columns: [string, string][]; empty: string }) { return rows.length === 0 ? <p className="p-5 text-sm text-slate-500">{empty}</p> : <div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="bg-slate-950 text-slate-500"><tr>{columns.map(([label]) => <th key={label} className="px-4 py-3 font-semibold">{label}</th>)}</tr></thead><tbody className="divide-y divide-slate-800">{rows.map((row, i) => <tr key={row.id ?? i}>{columns.map(([, key]) => <td key={key} className="px-4 py-3 text-slate-300">{display(row[key])}</td>)}</tr>)}</tbody></table></div>; }
function Select({ label, value, setValue, options }: { label: string; value: string; setValue: (value: string) => void; options: { value: string; label: string }[] }) { return <label className="text-xs font-semibold text-slate-400">{label}<select value={value} onChange={(e) => setValue(e.target.value)} className="mt-1 block min-w-40 rounded-lg border border-slate-700 bg-slate-950 p-2 text-sm text-slate-100">{options.map((o) => <option key={o.value} value={o.value}>{o.label}</option>)}</select></label>; }
function Input({ label, value, setValue, required = false }: { label: string; value: string; setValue: (value: string) => void; required?: boolean }) { return <label className="text-xs font-semibold text-slate-400">{label}<input required={required} value={value} onChange={(e) => setValue(e.target.value)} className="mt-1 block w-full rounded-lg border border-slate-700 bg-slate-950 p-2 text-sm text-slate-100" /></label>; }
function Date({ label, value, setValue }: { label: string; value: string; setValue: (value: string) => void }) { return <label className="text-xs font-semibold text-slate-400">{label}<input type="date" value={value} onChange={(e) => setValue(e.target.value)} className="mt-1 block rounded-lg border border-slate-700 bg-slate-950 p-2 text-sm text-slate-100" /></label>; }
function display(value: any) { if (!value) return "—"; return /At$|From$|Until$/.test(String(value)) ? new globalThis.Date(value).toLocaleString() : String(value).replaceAll("_", " "); }
function csv(value: unknown) { const text = String(value ?? ""); return /[",\n]/.test(text) ? `"${text.replaceAll('"', '""')}"` : text; }
function readable(error: unknown) { return error instanceof Error ? error.message : "Unable to load secure-area authorizations"; }
